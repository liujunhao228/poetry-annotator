重构自定义体系：
插件体系已经重构完成
还有数据管理体系需要重构
以及配置管理体系需要对应调整
请你编写重构方案
目前的情况是：
配置管理体系：
config\global\config.ini 其仅需要定义模型配置、日志配置、请求速率与熔断器配置等等基类
config\rules 需要迁移再定义“全局”层级的规则集，因为没有必要。后续将规则集放在“项目”层级定义
config\projects 需要舍弃，因为已经被部分替代
config\rules 需要舍弃，后续不再有“全局”层级的规则集，因为没有必要。我们需要将规则集完全放在“项目”层级定义
config\metadata 需要舍弃，因为简化目录结构后，该文件已经没有必要
config\active_project.json 需要删除，因为没有必要了。同样的，config\system 也需要删除。我们直接加载根目录下的project目录即可
E:\poetry-annotator\config\plugins.ini 需要删除，不再在全局注册插件，而是由project\plugins.ini注册
config\label 需要纳入管理（旧体系没有处理）

数据管理体系：
在“项目”层级定义数据结构、数据库管理、数据库初始化（包括待标注的数据导入，旧体系没有处理）等等逻辑
由项目配置文件指定数据来源、数据库路径
